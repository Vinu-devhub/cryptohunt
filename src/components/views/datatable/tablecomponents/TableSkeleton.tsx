import { Skeleton } from "@/components/ui/skeleton";

const TableSkeleton = () => {
  return (
    <div>
      <Skeleton className=" w-full h-96 rounded-lg bg-slate-800 " />
    </div>
  );
};

export default TableSkeleton;
